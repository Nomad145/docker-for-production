version: '3'

services:
  web:
    image: nginx:latest
    volumes:
      - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./public:/app/public
  fpm:
    image: php:7.2-fpm
    volumes:
      - ./:/app
